#include<bits/stdc++.h>

using namespace std;

int main() {
	int N;
	cin >> N;
	vector<int> a;
	vector<int> b;
	for (int i = 0; i < N; ++i) {
		cin >> a[i];
	}
	for (int i = 0; i < N; i++) cin >> b[i];
	
}