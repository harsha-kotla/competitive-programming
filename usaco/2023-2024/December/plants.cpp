#include<bits/stdc++.h>

using namespace std;

void solve() {
    int n;
    cin >> n;
    int h[n];
    int a[n];
    int t[n];
    for (int i = 0; i < n; i++) cin >> h[i];
    for (int i = 0; i < n; i++) cin >> a[i];
    for (int i = 0; i < n; i++) cin >> t[i];
    int ans = -1;
    
}

int main() {
    int T;
    cin >> T;
    while (T--) {
        solve();
    }
}